# --- Agent/editor local state (never commit) ---
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.superpowers/

# --- Throwaway debug probes ---
# One-off analysis scripts go here. NEVER commit hand-copied replicas of
# voxEx.html terrain/game code — they drift. If a probe needs real game
# functions, extract them by name like tools/terrain-node-checks.mjs does.
tools/scratch/

# --- OS noise ---
Thumbs.db
.DS_Store
desktop.ini
